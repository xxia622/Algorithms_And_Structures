# Implementations

Core data structures written from a blank file, from memory — the "write it before you read it" rep from the coding floor. Each one is diffed against a reference implementation afterwards; where they differ is the lesson.

| File | Structure | Written |
|---|---|---|
| `DynamicArray.cpp` | Dynamic array — `push_back`, `insert(i)`, `remove(i)`, 2× resize | 2026-07-19 |
